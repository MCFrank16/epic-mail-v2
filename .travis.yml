language: node_js
node_js:
- node

script: echo "CI is running......"

jobs:
  include:
    - stage: Produce Coverage
      node_js: node
      script: npm test

services:
  - postgresql

before_script:
  - psql -c 'create database epic_mail_db;' -U postgres
  - psql -c "create user epicmail with PASSWORD 'mutafrank';" -U postgres
  
after_success: npm run coverage